<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" 
    integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" 
    crossorigin="anonymous">
</head>
<body>
    <div class="container">
        <h1>
            My posts
        </h1>
        <a href="<%=login%>/new" class="btn btn-success">new post</a>
        <a href="/" class="btn btn-primary">Logout</a>
    </div>
    <div class="container">
        <% content.forEach(element => { %>
          <div class="card my-3">
            <div class="card-body">
              <div class="card-title">
                <h3><%= element.title %></h3>
              </div>
              <div class="card-text">
                <%= element.date.toLocaleDateString() %>
              </div>
              <div class="card-text">
                <%= element.description %>
              </div>
              <div class="card-text">
                <%= element.markdown %>
              </div>
            </div>
            <div class="container">
              <form action="/<%=login%>?_method=DELETE" method="POST" class="d-inline">
                <button class="btn btn-danger my-2 mx-2" type="submit">Delete</button>
              </form>
              <a href="/<%=login%>/edit" class="btn btn-secondary">edit</a>
            </div>
          </div>
       <% }) %>
      </div>
</body>
</html>